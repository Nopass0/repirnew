// repitnew/server/src/schemas/enums.ts
export enum EntityStatus {
  ACTIVE = "ACTIVE",
  ARCHIVED = "ARCHIVED",
  DELETED = "DELETED",
  CANCELLED = "CANCELLED",
}

export enum SidebarPage {
  Main = "main",
  StudentCards = "student_cards",
  ClientCards = "client_cards",
  GroupCards = "group_cards",
}
